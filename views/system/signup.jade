!!! 5
html(lang="de")
	head
		meta(charset="utf-8")
		title DCM Database Manager
		link(rel="stylesheet", href="//netdna.bootstrapcdn.com/bootstrap/3.0.2/css/bootstrap.min.css")
		link(rel="stylesheet", href="//netdna.bootstrapcdn.com/font-awesome/4.0.3/css/font-awesome.min.css")
		style.
			body {
				padding: 5em;
			}
	body
		.container
			.col-sm-6.col-sm-offset-3
				h1
					span.fa.fa-sign-in &nbsp;
					| Registrierung
				if (message.length > 0)
					.alert.alert-danger=message
				form(action="/signup", method="post")
					.form-group
						label(for="un") Name
						input#un(type="text", name="display_name").form-control
					.form-group
						label(for="email") E-Mail
						input#email(type="text", name="email", required).form-control
					.form-group
						label(for="pass") Passwort
						input#pass(type="password", name="password", required).form-control
					button(type="submit").btn.btn-warning.btn-lg registrieren
				hr
				p Bereits registriert?&nbsp;
					a(href="/signup") Anmelden!
				p Zurück zum 
					a(href="/") Anfang
			
				